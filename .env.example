# Server Port
PORT=3001

# AES-256-CBC Encryption Key for sensitive fields (NIK)
# Must be exactly 32 characters in length
ENCRYPTION_KEY=klinik-secure-nik-key-32-bytes!!

# Optional custom base URL for frontend Vite builds
# VITE_API_BASE_URL=http://localhost:3001/api
